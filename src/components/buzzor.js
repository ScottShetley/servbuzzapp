import React from "react";

const Buzzor = props => (
  <div>
    <h1 className="cover-heading">buzzor works</h1>
    <button className="btn tbn-primary">Buzz</button>
  </div>
);

export default Buzzor;
